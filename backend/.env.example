NODE_ENV=development
PORT=4000

# Frontend origin for CORS
WEB_ORIGIN=http://localhost:5173

# Database
MONGO_URI=mongodb://localhost:27017/storyuu

# JWT
JWT_ACCESS_SECRET=replace-me-with-a-long-random-string
JWT_REFRESH_SECRET=replace-me-with-a-different-long-random-string
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Stripe (stubbed in Phase 0; wire when keys are available)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID_MONTHLY=
STRIPE_PRICE_ID_YEARLY=

# Object storage (stubbed)
STORAGE_PROVIDER=stub
S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=

# Email (stubbed)
EMAIL_PROVIDER=stub
SENDGRID_API_KEY=
EMAIL_FROM="Storyuu <hello@storyuu.local>"
